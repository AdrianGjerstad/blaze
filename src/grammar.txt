expr          : KEYWORD:VAR IDENTIFIER (COMMA IDENTIFIER)* EQ expr
              : term ((PLUS|MINUS) term)*

term          : factor ((MUL|DIV) factor)*

factor        : (PLUS|MINUS) factor
              : power

power         : unit (POW factor)*

unit          : INT|FLOAT|IDENTIFIER
              : LPAREN expr RPAREN